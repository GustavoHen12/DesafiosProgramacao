#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
    cin.tie(0);
    ios_base::sync_with_stdio(0);
    // implemente sua lógica

    ll n, q;
    cin >> n >> q;
    vector<vector<int>> planetas;

    // Monta
    for(ll u = 0; u < q; u++){
        int v; cin >> v;
    }

}